import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
  resourceContainer: {
    maxWidth: "100%",
    maxHeight: 400,
    minHeight: 200,
    minWidth: 150,
  },
}));
